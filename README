samples for nashorn javafx.

made using bower components and shows:
testing with jasmine-nashorn.
repl and live reloading of js, fxml and css.

get the repo :
git@github.com:pickledonion/todo-fx.git

cd todo-fx

first install components with:
bower install

( bower is a javascript package manager http://bower.io/ )


then move the the folder with the scripts in:
cd src/

then run with any of:
./todo-fx.js

./outliner.js

./tableview.js

./date-picker.js

there is also a  netbeans project you can run the examples from too.
you can choose different examples to run by selecting a different configuration.
